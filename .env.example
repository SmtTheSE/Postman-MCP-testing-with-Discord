# VoiceDrop — copy to discord-mcp/.env (or project root .env for Vercel)

DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
SESSION_SECRET=

# Discord Developer Portal → OAuth2 → Redirects:
# http://localhost:5173/api/auth/callback
